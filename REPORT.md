# MLP 기반 방정식 근사 풀이의 속도·정확도 연구 보고서

작성일: 2026-08-17  
프로젝트: 카르다노 학습 데이터로 훈련한 삼차방정식 MLP

## 초록

본 연구는 실수 계수 삼차방정식

\[
ax^3+bx^2+cx+d=0,\qquad a\ne0
\]

의 세 복소근을 다층 퍼셉트론(MLP)으로 근사하고, 전통적인 카르다노 공식 및
`numpy.roots`와 속도와 정확도를 비교하였다. 학습 정답은 프로젝트에서 직접
구현한 카르다노 공식으로 생성했으며, 근의 순서가 정의되지 않는 문제를 해결하기
위해 세 근의 6가지 순열 중 최소 손실을 선택하는 permutation-invariant loss를
사용하였다.

13th Gen Intel Core i5-1340P CPU에서 20,000개 방정식을 처리한 결과, MLP 배치는
식당 4.386 μs로 `numpy.roots`보다 20.00배 빨랐다. MLP 출력을 초기값으로 사용해
Newton 보정을 4회 적용한 방법은 식당 13.838 μs로 `numpy.roots`보다 6.34배
빨랐다. 근접 중근, 정확한 중근, \(10^{-12}\sim10^{12}\)의 근 크기를 포함한
난제 세트에서 MLP+Newton은 세 근의 최대 무차원 오차가 0.01 이하인 비율
99.20%를 기록했다.

그러나 완전히 벡터화한 카르다노 공식은 식당 0.511 μs로 MLP보다 약 8.58배
빨랐으며 정확도도 더 높았다. 따라서 MLP의 속도 우위는 단일 방정식이나 최적화된
고정 차수 공식이 아니라, 다수의 방정식을 한 번에 처리하면서 행 단위 반복 수치법과
비교하고 일정 수준의 근사오차를 허용할 때 성립한다.

## 1. 연구 목적과 질문

본 연구의 질문은 다음과 같다.

1. 삼차방정식 계수에서 세 복소근으로 가는 함수를 MLP가 학습할 수 있는가?
2. MLP는 어느 배치 크기부터 전통적인 수치 풀이보다 빨라지는가?
3. 근접 중근과 극단적인 근 크기를 포함한 난제에서도 유용한 근사값을 제공하는가?
4. MLP 단독보다 `MLP 초기값 + 수치 보정` 방식이 더 실용적인가?
5. 삼차방정식 이외에는 어떤 방정식군에서 더 큰 속도 이점을 기대할 수 있는가?

여기서 “빠르다”는 모델 학습 시간을 제외한 추론 단계의 wall-clock time이 작다는
뜻이다. 신경망은 먼저 많은 학습 비용을 지불하고 이후 반복 계산에서 이를 회수하는
surrogate model이므로, 일회성 풀이와 대량 반복 풀이를 구분하였다.

## 2. 수학적 전처리

### 2.1 Monic 변환과 방정식 크기 불변성

원래 식을 선도계수 \(a\)로 나누어

\[
x^3+Bx^2+Cx+D=0,
\qquad
B=\frac ba,\ C=\frac ca,\ D=\frac da
\]

으로 만든다. 이 과정은 방정식 전체를 임의의 0이 아닌 상수로 곱해도 입력과 근이
달라지지 않도록 한다.

### 2.2 변수 크기 정규화

변수 배율을

\[
s=\max\left(|B|,\sqrt{|C|},\sqrt[3]{|D|}\right)
\]

로 두고 \(x=sz\)를 대입한다. 모든 배율 후보가 0인 \(x^3=0\)에서는 \(s=1\)로
정의한다. 모델이 보는 식은

\[
z^3+B_nz^2+C_nz+D_n=0
\]

이며

\[
B_n=\frac{B}{s},\qquad
C_n=\frac{C}{s^2},\qquad
D_n=\frac{D}{s^3}
\]

이다. 따라서 입력은 `[Bn, Cn, Dn]` 세 개이고 각 절댓값은 1 이하이다. MLP가
예측한 \(z\) 근은 마지막에 \(s\)를 곱해 원래 \(x\) 근으로 복원한다.

## 3. 학습 정답 생성

### 3.1 카르다노 공식

\(z=y-B_n/3\)을 대입하면 depressed cubic

\[
y^3+py+q=0
\]

을 얻는다. 판별에 사용한 값은

\[
\Delta=\left(\frac q2\right)^2+\left(\frac p3\right)^3
\]

이다.

- \(\Delta>0\): 한 실근과 켤레복소근 한 쌍
- \(\Delta<0\): 서로 다른 세 실근
- \(\Delta\approx0\): 이중근 또는 삼중근

음수 실수의 세제곱근은 `np.cbrt`로 처리하고, 세 실근 구간에서는 복소 세제곱근의
가지 선택 문제를 피하기 위해 카르다노 공식과 동치인 삼각함수 표현을 사용했다.
판별식 0 판정에는 고정 절대오차가 아니라 판별식을 구성하는 두 항의 크기에 비례한
상대 허용오차를 사용했다.

### 3.2 데이터 구성

학습·검증·시험 데이터는 서로 다른 seed로 독립 생성했다.

| 분할 | 샘플 수 |
|---|---:|
| 학습 | 100,000 |
| 검증 | 10,000 |
| 시험 | 10,000 |

데이터에는 다음 regime을 혼합했다.

- 서로 다른 세 실근
- 한 실근과 켤레복소근
- 근접 중근
- 정확한 이중근·삼중근
- 정규화 계수를 직접 무작위 표본화한 방정식

근을 먼저 구성한 샘플도 최종적으로 계수로 변환한 뒤, 원래 구성근을 그대로 라벨로
재사용하지 않고 직접 구현한 카르다노 solver로 다시 풀어 라벨을 만들었다.

## 4. MLP 설계

### 4.1 구조

모델 구조는 다음과 같다.

\[
3\rightarrow128\rightarrow256\rightarrow256\rightarrow128\rightarrow6
\]

출력 여섯 개는 다음 세 복소근의 실수부와 허수부이다.

\[
(\Re z_1,\Im z_1),
(\Re z_2,\Im z_2),
(\Re z_3,\Im z_3)
\]

은닉층 활성화 함수는 SiLU이며 전체 학습 파라미터 수는 132,998개다. 입력과 출력
차원이 작고 공간·시간 순서가 없는 고정 길이 회귀 문제이므로 CNN, RNN,
Transformer보다 완전연결 MLP가 적합하다. 복소근을 극좌표로 출력하면 위상 경계가
불연속이 되므로 Cartesian 실수부·허수부 표현을 사용했다.

### 4.2 순열 불변 지도 손실

삼차방정식의 세 근은 순서가 없는 집합이다. 따라서 세 근의 가능한 \(3!=6\)개
대응을 모두 조사해 샘플마다 가장 작은 Smooth L1 손실을 선택했다.

\[
L_{match}
=\min_{\pi\in S_3}\frac16
\sum_{i=1}^3
\operatorname{SmoothL1}(\hat z_i,z_{\pi(i)})
\]

### 4.3 다항식 잔차와 비에타 손실

예측근이 실제 식을 만족하도록 정규화 잔차 손실을 추가했다.

\[
\rho(z)=
\frac{|z^3+B_nz^2+C_nz+D_n|}
{|z|^3+|B_n||z|^2+|C_n||z|+|D_n|}
\]

또한 세 출력이 같은 근 하나로 붕괴하는 현상을 감지하기 위해

\[
z_1+z_2+z_3=-B_n,
\]

\[
z_1z_2+z_1z_3+z_2z_3=C_n,
\]

\[
z_1z_2z_3=-D_n
\]

을 이용한 비에타 손실을 사용했다. 전체 손실은

\[
L=L_{match}+0.05L_{res}+0.02L_{Vieta}
\]

이다.

### 4.4 학습 하이퍼파라미터

| 항목 | 값 |
|---|---:|
| optimizer | AdamW |
| 초기 learning rate | 0.001 |
| weight decay | \(10^{-5}\) |
| batch size | 1,024 |
| 최대 epoch | 100 |
| Smooth L1 beta | 0.05 |
| gradient clipping | 1.0 |
| auxiliary warm-up | 10 epoch |
| scheduler | ReduceLROnPlateau, factor 0.5, patience 4 |
| early stopping patience | 18 |
| seed | 20260816 |

최적 검증 epoch는 73이었고, 최적 검증 무차원 root RMSE는 0.034106이었다.

## 5. 기본 모델 정확도

독립 시험 세트 10,000개에서 실수 계수 구조를 반영한 MLP 출력은 다음 결과를
기록했다.

| 지표 | 값 |
|---|---:|
| 무차원 root MAE | 0.015186 |
| 무차원 root RMSE | 0.036723 |
| 샘플 최대 근 오차 median | 0.012801 |
| 샘플 최대 근 오차 p95 | 0.054199 |
| 샘플 최대 근 오차 p99 | 0.170972 |

근접 중근 regime의 RMSE는 0.064676으로 전체 regime 중 가장 컸다. 이는 중근
근처에서 작은 계수 변화가 큰 forward root 변화로 이어지는 문제 자체의 나쁜
조건수와 관련이 있다. MLP는 이 수학적 불안정성을 제거하지 못한다.

## 6. 속도 실험 방법

### 6.1 환경

- CPU: 13th Gen Intel Core i5-1340P
- 논리 구성: 12 cores / 16 threads
- PyTorch CPU threads: 12
- 장치: CPU
- 일반 속도 세트: 20,000개
- 난제 정확도 세트: 5,000개
- 속도 반복: warm-up 후 5회 median
- 단일 식 반복: 50회 median
- checkpoint 최초 적재: 0.0109초, 배치 시간에서는 제외
- 전처리, tensor 변환, 추론, 후처리, CPU 결과 복사는 시간에 포함

### 6.2 비교 방법

1. 완전히 벡터화한 카르다노 공식
2. `numpy.roots`를 각 행에 반복 호출
3. 구조 후처리를 포함한 MLP 배치
4. MLP 출력에서 복소 Newton 보정 4회
5. 신뢰도가 낮은 행만 카르다노로 교체하는 MLP+Cardano hybrid

Newton 보정에서는 도함수가 지나치게 작으면 갱신을 건너뛰고, 잔차가 줄지 않는
step은 최대 8회의 backtracking으로 축소한다.

## 7. 속도 결과

### 7.1 20,000개 배치

| 방법 | μs/식 | 식/초 | `numpy.roots` 대비 |
|---|---:|---:|---:|
| 벡터화 Cardano | 0.511 | 1,955,627 | 171.54배 |
| `numpy.roots` 행 반복 | 87.715 | 11,401 | 1.00배 |
| MLP 배치 | 4.386 | 228,008 | **20.00배** |
| MLP + Newton 4회 | 13.838 | 72,262 | **6.34배** |
| MLP+Cardano hybrid | 12.052 | 82,974 | **7.28배** |

MLP는 일반적인 행 반복 companion-matrix 풀이보다 빨랐지만, 고정 차수 문제에
맞추어 완전히 벡터화한 카르다노 공식보다 느렸다.

### 7.2 속도 교차점

배치 크기 1, 4, 16, 64, 256, 1,024, 4,096, 16,384 및 20,000에서 각 방법을
반복 측정했다.

| 비교 | MLP 최초 승리 | MLP+Newton 최초 승리 |
|---|---:|---:|
| 식마다 호출한 Cardano | 배치 **4** | 배치 **16** |
| `numpy.roots` 행 반복 | 배치 **64** | 배치 **256** |
| 벡터화 Cardano | 측정 범위에서 없음 | 측정 범위에서 없음 |

20,000개 교차점 측정에서 식마다 호출한 카르다노는 식당 약 466.45 μs,
MLP는 약 4.40 μs였다. 이때 약 106배 차이가 발생했다. 이것은 카르다노 공식의
수학적 연산량보다 Python 행 반복과 벡터화 행렬곱 사이의 구현 차이가 크게 반영된
결과다.

### 7.3 단일 방정식

| 방법 | warm median latency |
|---|---:|
| 벡터화 Cardano API | 569.45 μs |
| `numpy.roots` | 195.25 μs |
| MLP | 1,740.30 μs |
| MLP + Newton 4회 | 2,975.20 μs |
| hybrid | 2,982.20 μs |

단일 식에서는 tensor와 모델 호출 오버헤드 때문에 MLP가 가장 느렸다. 따라서
MLP의 사용 조건에는 대량 배치가 필수적이다.

## 8. 난제 세트 결과

난제 세트는 다음과 같이 구성했다.

- 50%: 상대 근 간격 \(10^{-10}\sim10^{-3}\)인 근접 중근
- 20%: 정확한 이중근 또는 삼중근
- 근 크기: \(10^{-12}\sim10^{12}\)
- 나머지: 세 실근, 켤레복소근, 무작위 계수

### 8.1 RMSE

| 방법 | 일반 20,000개 | 난제 5,000개 |
|---|---:|---:|
| Cardano 기준 | 0 | 0 |
| `numpy.roots` | 2.19×10⁻⁷ | 4.47×10⁻⁷ |
| MLP 단독 | 0.038020 | 0.036435 |
| MLP + Newton 4회 | 0.043080 | 0.041052 |
| MLP+Cardano hybrid | **0.003290** | **0.001658** |

Newton 보정은 대부분의 샘플을 매우 정확하게 만들지만, 일부 초기값이 다른 근으로
수렴하는 큰 outlier 때문에 전체 RMSE는 오히려 증가했다. 따라서 평균 RMSE만으로
Newton 보정의 실용성을 판단하지 않고 허용오차 성공률을 함께 보았다.

### 8.2 허용오차 성공률

성공은 각 방정식에서 세 근을 최적으로 일대일 대응시킨 후, 세 근 모두의 최대
오차가 지정된 값 이하인 경우로 정의했다. 오차는 변수 크기 \(s\)로 나눈 무차원
\(z\) 공간 기준이다.

| 방법 | 최대 오차 0.01 이하: 일반 | 최대 오차 0.01 이하: 난제 |
|---|---:|---:|
| MLP 단독 | 40.67% | 13.64% |
| MLP + Newton 4회 | **98.21%** | **99.20%** |
| MLP+Cardano hybrid | **99.29%** | **99.84%** |

MLP+Newton은 난제 세트에서 99.20%의 식을 최대 무차원 근 오차 0.01 이내로
풀면서 `numpy.roots`보다 6.34배 빨랐다. 이것이 본 실험에서 발견한 가장 실용적인
MLP 속도 우위 조건이다.

## 9. 연구 질문에 대한 답

### 9.1 MLP가 고전 풀이보다 빠른 경우

다음 조건이 동시에 만족되면 빠르다.

1. 모델이 이미 학습되고 메모리에 적재되어 있다.
2. 같은 형태의 방정식을 최소 수십~수백 개 이상 모아 처리한다.
3. 비교 대상이 식마다 반복 호출되는 `numpy.roots` 또는 scalar solver다.
4. 모든 자리의 정확값이 아니라 사전에 정한 근사오차를 허용한다.
5. 학습 범위와 실제 입력 분포가 충분히 비슷하다.

이 CPU에서 순수 MLP는 배치 64부터 `numpy.roots`보다 빨랐으며, 정확도를 크게
높인 MLP+Newton은 배치 256부터 빨랐다.

### 9.2 MLP가 빠르지 않은 경우

- 한두 개의 방정식만 푸는 경우
- 완전히 벡터화한 카르다노 공식을 사용할 수 있는 경우
- 정확한 모든 자리 또는 검증된 오차 한계가 필요한 경우
- 학습 분포 밖의 계수 또는 구조가 입력되는 경우
- 중근 경계에서 개별 forward root의 높은 정확도가 반드시 필요한 경우

### 9.3 “어려운 방정식”의 두 의미

계산량이 커서 어려운 문제와 조건수가 나빠서 어려운 문제는 구분해야 한다.

- **계산량이 큰 반복 문제:** 신경망 surrogate가 큰 속도 향상을 낼 수 있다.
- **특이점·중근 때문에 불안정한 문제:** 입력에서 출력으로 가는 함수 자체가
  민감하므로 MLP도 어려움을 겪는다.

본 실험에서 MLP는 중근의 수학적 불안정성을 해결하지 않았다. 대신 빠른 초기값을
만들고 Newton 또는 Cardano 검증 경로와 결합해 전체 처리량을 높였다.

## 10. 다른 방정식으로의 확장 가능성

### 10.1 저차 다항식

2~4차 다항식은 공식과 벡터화 구현이 이미 매우 빠르므로 MLP의 연구·교육적 가치는
있어도 실용적인 속도 우위는 작다. 5차 이상에서는 companion matrix, Jenkins–Traub,
Aberth와 비교할 수 있지만, 차수가 증가할수록 출력 근 집합의 순열과 근접근
조건수가 더 어려워진다. 순수 MLP보다는 MLP 초기값과 Aberth/Newton 보정의 결합이
적합하다.

### 10.2 비선형 연립방정식

다음 연구 대상으로 가장 적합하다. 예를 들어

\[
\begin{cases}
x^2+y^2=r^2,\\
e^x+y=k
\end{cases}
\]

에서 \((r,k)\)가 계속 바뀌면 고전 Newton법은 각 문제마다 Jacobian 계산과 선형계
풀이를 반복한다. MLP가 \((r,k)\mapsto(x,y)\) 초기값을 예측하고 Newton을 1~3회만
수행하면 삼차방정식보다 큰 가속 가능성이 있다.

### 10.3 초월방정식

Kepler 방정식, Colebrook 방정식, 다이오드 회로식처럼 매개변수가 반복적으로
바뀌는 초월방정식은 후보가 될 수 있다. 다만 스칼라 Newton법도 매우 빠르므로
MLP가 이기려면 큰 배치와 제한된 입력 범위가 필요하다. 작은 lookup table,
Chebyshev 근사, 유리함수 근사도 함께 기준선으로 비교해야 공정하다.

### 10.4 ODE와 PDE

가장 큰 속도 향상이 예상되는 영역은 매개변수화된 미분방정식이다. 고전 PDE
solver는 새로운 초기조건, 경계조건, 물성치마다 격자 전체를 다시 계산하지만,
학습된 neural operator는 하나의 forward pass로 해의 장 전체를 예측할 수 있다.

DeepONet은 입력 함수에서 출력 함수로 가는 비선형 연산자를 학습하도록 설계되었고,
ODE·PDE의 암시적 solution operator를 포함한 다양한 문제에 적용되었다. Fourier
Neural Operator 연구는 Burgers 방정식, Darcy flow, Navier–Stokes 방정식에서
전통 solver 대비 최대 세 자릿수 배의 속도 향상을 보고했다. 이는 특정 데이터,
해상도, 장치 및 허용오차에서 얻은 논문 결과이며 모든 PDE에 자동으로 적용되는
보장은 아니다.

## 11. 권장 후속 연구

### 11.1 1단계: 비선형 연립방정식 MLP+Newton

본 프로젝트를 가장 자연스럽게 확장하는 실험이다.

1. 매개변수 2~6개를 입력으로 받는 비선형 연립방정식군을 선정한다.
2. SciPy 또는 고정밀 Newton solver로 학습 정답을 생성한다.
3. 해가 여러 개인 경우 순열 불변 손실 또는 해의 branch 분류를 사용한다.
4. MLP 단독, MLP+Newton, 무작위 초기값 Newton, continuation을 비교한다.
5. 배치 크기별 최초 속도 교차점과 허용오차 성공률을 측정한다.

권장 성공 기준은 다음과 같다.

- 최소 10,000개 독립 시험 문제
- 최대 해 오차 \(10^{-3}\) 이하 성공률 99% 이상
- 고전 행 반복 solver 대비 3배 이상 처리량
- 발산 또는 잘못된 branch 선택 0.1% 이하

### 11.2 2단계: 1차원 Burgers 또는 열전도 PDE

PDE에서는 단순 MLP보다 DeepONet 또는 FNO를 사용하는 것이 적합하다. 입력은
초기조건·경계조건·물성치이고, 출력은 일정한 격자의 전체 해이다. 유한차분법을
정답 기준으로 사용하고 다음을 비교해야 한다.

- 학습 데이터 생성까지 포함한 총비용과 break-even query 수
- 단일 query 및 대량 batch latency
- \(L_2\), \(L_\infty\), 보존법칙 오차
- 학습 범위 안·밖의 일반화
- 장시간 적분에서 누적 오차와 안정성

## 12. 한계

1. 속도 수치는 한 CPU와 현재 PyTorch/NumPy 환경에서 측정한 값이며 다른
   하드웨어에서는 달라진다.
2. 학습 시간은 추론 속도표에 포함하지 않았다. query 수가 작으면 학습 비용을
   회수하지 못한다.
3. 카르다노 라벨과 모델은 실수 계수 삼차방정식만 대상으로 한다.
4. MLP의 평균 정확도는 전통 solver보다 낮고 극단적인 outlier가 존재한다.
5. Newton 보정은 서로 다른 초기값을 같은 근으로 수렴시킬 수 있다.
6. 완전히 벡터화한 카르다노는 현재 CPU에서 여전히 가장 빠르고 정확하다.
7. 신경망은 학습 분포 밖 입력에서 신뢰할 수 없으므로 잔차·비에타 검사와 fallback이
   필요하다.
8. PDE 관련 가속 수치는 본 프로젝트의 직접 실측이 아니라 선행 연구 결과다.

## 13. 재현 방법

환경 설치와 학습:

```powershell
python -m pip install -e .
python train.py
```

예측:

```powershell
python predict.py 1 -6 11 -6 --show-cardano --polish-steps 4
```

속도·난제·교차점 벤치마크:

```powershell
python benchmark.py `
  --samples 20000 `
  --hard-samples 5000 `
  --repeats 5 `
  --single-repeats 50
```

테스트:

```powershell
python -m pytest -q
```

작성 시점의 자동 테스트 결과는 41 passed이다. 전체 벤치마크 원자료는
`artifacts/speed_benchmark.json`, 기본 모델 시험 지표는
`artifacts/metrics.json`에 저장되어 있다.

## 14. 결론

삼차방정식 MLP는 정확한 고전 공식을 대체하지 못했지만, 대량 배치의 근사 계산에서
명확한 속도 우위 조건을 확인했다. 순수 MLP는 배치 64부터 `numpy.roots`를
추월했고, MLP+Newton은 배치 256부터 추월했다. 20,000개 배치에서 MLP+Newton은
`numpy.roots`보다 6.34배 빨랐고, 난제 세트의 99.20%를 최대 무차원 근 오차
0.01 이내로 풀었다.

반면 벡터화 카르다노 공식은 MLP보다 빠르고 정확했다. 따라서 이 연구의 핵심
결론은 “MLP가 언제나 고전 풀이보다 빠르다”가 아니라 다음과 같다.

> 이미 학습된 모델로 같은 형태의 방정식을 대량 처리하고 일정한 근사오차를
> 허용할 때, MLP 또는 MLP+소수 회 수치 보정은 행 단위 반복 solver보다 높은
> 처리량을 제공할 수 있다.

향후에는 계산량이 더 큰 매개변수화 비선형 연립방정식과 PDE로 확장하는 것이
타당하다. 특히 MLP를 완전한 대체 solver로 사용하기보다 빠른 초기값·surrogate로
사용하고, 수치 잔차와 고전 solver fallback으로 신뢰성을 확보하는 방식이 가장
실용적이다.

## 참고문헌 및 자료

1. Lu, L. et al., “Learning nonlinear operators via DeepONet based on the
   universal approximation theorem of operators,” *Nature Machine
   Intelligence* 3, 218–229 (2021).
   <https://doi.org/10.1038/s42256-021-00302-5>
2. Li, Z. et al., “Fourier Neural Operator for Parametric Partial Differential
   Equations,” *ICLR 2021*. <https://openreview.net/pdf?id=c8P9NQVtmnO>
3. NumPy documentation, `numpy.roots`.
   <https://numpy.org/doc/stable/reference/generated/numpy.roots.html>
4. PyTorch documentation, `SmoothL1Loss`.
   <https://docs.pytorch.org/docs/stable/generated/torch.nn.SmoothL1Loss.html>
5. 프로젝트 상세 설계: `README.md`
6. 학습 지표: `artifacts/metrics.json`
7. 속도·정확도 원자료: `artifacts/speed_benchmark.json`
